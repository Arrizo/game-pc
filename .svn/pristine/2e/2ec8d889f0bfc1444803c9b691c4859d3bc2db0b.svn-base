function getLanguagle() {
  let lang = navigator.language || navigator.userLanguage
  if (lang.indexOf('en') > -1) {
    return 'en'
  } else if (lang.indexOf('zh') > -1) {
    return 'zh'
  } else {
    return 'en'
  }
}
export default getLanguagle;
